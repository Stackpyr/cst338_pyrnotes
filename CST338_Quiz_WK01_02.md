# CST338 Quiz — Week 1, Set B

Based on: CST338_Notes_wk01.md

Instructions: circle or type your answer letter next to each question. When you're ready, ask me to grade this quiz before generating the next one.

**1. Why must the `main` method's parameter be declared as `String[] args` rather than some other type?**
A. It's just convention with no real requirement
B. It's required by the JVM as part of the entry-point method signature
C. It only affects performance
D. It allows `main` to return a `String`

Your answer: ___

**2. Which mechanism does the encapsulation section describe for controlling access to an object's state?**
A. Static methods
B. Setters/getters combined with `private` fields
C. Public fields only
D. Constructors alone

Your answer: ___

**3. Per the notes, an object is best described as:**
A. A primitive value
B. An instance of a class that holds data and methods and maintains its own state
C. A synonym for a Java file
D. A static utility class

Your answer: ___

**4. In UML, which relationship corresponds to Java's `extends` keyword?**
A. Association
B. Realization
C. Generalization
D. Aggregation

Your answer: ___

**5. In JUnit, what does the `@Test` annotation do?**
A. It documents the method but has no functional effect
B. It marks a method to be run automatically as a unit test
C. It marks a method as an entry point for the JVM
D. It replaces the need for assertions

Your answer: ___
