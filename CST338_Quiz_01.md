# CST338 Quiz 1

Based on: CST338_Notes_wk01.md (Java Primer, Encapsulation, Git Workflow)

Instructions: circle or type your answer letter next to each question. When you're ready, ask me to grade this quiz before generating the next one.

**1. In the git workflow used in class, what is the purpose of running `git pull origin main` while checked out on `main`, before merging it into a feature branch?**
A. To bring the local main branch up to date with the remote before merging it into the feature branch
B. To delete the feature branch
C. To push feature branch changes to main directly
D. To create a new repository

Your answer: ___

**2. Per the notes, what happens if a Java file's name doesn't match its public class name?**
A. Nothing, Java ignores file names
B. The code compiles but runs slower
C. It causes a compile error
D. The class is automatically renamed

Your answer: ___

**3. According to the notes, what is the difference between a "function" and a "method" in Java?**
A. There is no difference, they're the same thing
B. A method is a function that belongs to an object; a function isn't necessarily tied to an object
C. Functions can only return void, methods can return any type
D. Methods are used only in main, functions are used everywhere else

Your answer: ___

**4. Which of these best matches the definition of encapsulation given in the notes?**
A. Writing all your code in a single file
B. A mechanism for restricting direct access to an object's components and bundling data with the methods that operate on it
C. A way to speed up compilation
D. The process of converting source code into byte code

Your answer: ___

**5. Per the notes, what is an object in Java?**
A. A primitive data type
B. A collection of data and methods that operate on that data; an instance of a class
C. A synonym for "class"
D. A file that contains only methods, no data

Your answer: ___
